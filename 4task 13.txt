#include <iostream>
using namespace std;

int main() {
    int n, x, even=0, odd=0;
    cin >> n;
    for(int i = 1; i <= n; i++){
        cin >> x;
        if(i % 2 == 0) even += x;
        else odd += x;
    }
    cout << odd << " " << even;
}
